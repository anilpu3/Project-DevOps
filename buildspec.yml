version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17
  post_build:
    commands:
      - mvn package
      - mv target/vprofile-v2.war vprofile-v2.war
artifacts:
  files:
    - vprofile-v2.war
    - .ebextensions/**/*
  

