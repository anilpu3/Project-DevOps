version: 0.2

phases:
  install:
    runtime-versions:
      java: openjdk8
      java: corretto11
  post_build:
    commands:
      - mvn package - DskipTests
      - mv target/vprofile-v2.war vprofile-v2.war
artifacts:
  files:
    - vprofile-v2.war
    - .ebextensions/**/*
  

